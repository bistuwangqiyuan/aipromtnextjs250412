1:新建netlify文件，内容参考：
[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NEXT_TELEMETRY_DISABLED = "1"
  NODE_VERSION = "18"

[[plugins]]
  package = "@netlify/plugin-nextjs" 
# 通过上传github 
# netlify网站中新建site
# 设置环境变量等，例如各种pai密钥等。尤其是supabase的api